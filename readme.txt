pip install selenium

gerar executavel:
pip install pyinstaller
pyinstaller --onefile relatorio.py

observação:
o executavel incluirá todas as bibliotecas python instaladas, então sugiro
criar um novo ambiente virtual apenas com as bibliotecas usadas no código antes de criar o executável.

